import './Title.css';

function Title(title) {
  return `
    <div class="title">
        <h1>${title}</h1>
        <div class="title-decoration"><br /></div>
    </div>
      `;
}

export default Title;
