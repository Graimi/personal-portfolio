import './Title.css';

function Title(title) {
  return `
    <div class="page-title">
        <h1>${title}</h1>
        <div class="decoration"><br /></div>
    </div>
      `;
}

export default Title;
